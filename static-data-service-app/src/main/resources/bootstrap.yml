server:
  port: 8802

spring:
  application:
    name: static-data-service
  cloud:
    config:
      discovery:
        enabled: true
management.security.enabled: false

eureka:
 client:
  serviceUrl:
   defaultZone: http://localhost:8761/eureka/
 healthcheck:
   enabled: true